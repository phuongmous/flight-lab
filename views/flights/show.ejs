<%- include('../partials/header') %>

<br><br><h2>Flight Information</h2>
<section id="show-page">
    <div>Airline: </div>
    <div><%= flight.airline %></div>
    <div>Airport: </div>
    <div><%= flight.airport %></div>
    <div>Flight No: </div>
    <div><%= flight.flightNo %></div>
    <div>Departs: </div>
    <div><%= flight.departs %></div>
</section>

<br><br><h2>Destinations</h2>
<form id="add-destination-form" method="post" 
action="/flights/<%= flight._id %>/destinations">
    <label>Airport:</label>
        <select name="airport">
            <option>AUS</option>
            <option>DFW</option>
            <option>DEN</option>
            <option>LAX</option>
            <option>SAN</option>
        </select>
    <br>
    <label>Arrival:
        <input type="datetime-local" name="arrival">
    </label>
    <br>
    <input type="submit" value="Add Destination">
</form>

<% if (flight.destinations.length) { %>
    <table>
        <thead>
            <tr>
                <th>Airport</th>
                <th>Arrival</th>
            </tr>
        </thead>
        <tbody>
            <% flight.destinations.forEach(function(f) { %>
                <tr>
                    <td><%= f.airport %></td>
                    <td><%= f.arrival %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
<% } else { %>
    <h4>No Arrivals Yet</h4>
<% } %>

<br><br><h2>Tickets</h2>
<% console.log('flight ticket', flight) %>
<% if (tickets.length) { %>
    <table>
        <thead>
            <tr>
                <th>Seat</th>
                <th>Price</th>
            </tr>
        </thead>
        <tbody>
            <% tickets.forEach(function(t) { %>
                <tr>
                    <td><%= t.seat %></td>
                    <td><%= t.price %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
<% } else { %>
    <h4>No Tickets Yet</h4>
<% } %>
<br>
<a href="/flights/<%= flight._id %>/tickets/new" class="button-link">New Ticket</a>

<%- include('../partials/footer') %>

